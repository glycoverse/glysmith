% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step.R
\name{step_dea_ttest}
\alias{step_dea_ttest}
\title{Step: Differential Expression Analysis (DEA) using t-test}
\usage{
step_dea_ttest(on = "exp", ...)
}
\arguments{
\item{on}{Name of the experiment data in \code{ctx$data} to run analysis on.
Default is \code{"exp"} for differential expression analysis.
Use \code{"trait_exp"} for differential trait analysis.
Use \code{"motif_exp"} for differential motif analysis.}

\item{...}{Step-specific arguments passed to \code{glystats::gly_ttest()}.
Use the format \code{pkg.func.arg}.}
}
\value{
A \code{glysmith_step} object.
}
\description{
Run differential analysis using t-test via \code{glystats::gly_ttest()},
then filter the experiment to keep only the differentially expressed variables using \code{glystats::filter_sig_vars()}.
By default, this runs DEA on the main experiment (\code{exp}), but can be configured
to run on derived traits (\code{trait_exp}) or other experiment objects.
Only use this method for experiments with 2 groups.
}
\details{
Data required:
\itemize{
\item Depends on \code{on} parameter (default: \code{exp})
}

Data generated:
\itemize{
\item \code{dea_res}: The DEA results (if \code{on = "exp"}, default)
\item \code{dta_res}: The DTA results (if \code{on = "trait_exp"})
\item \code{dma_res}: The DMA results (if \code{on = "motif_exp"})
\item \code{sig_exp}: The filtered experiment (if \code{on = "exp"}, default)
\item \code{sig_trait_exp}: The filtered trait experiment (if \code{on = "trait_exp"})
\item \code{sig_motif_exp}: The filtered motif experiment (if \code{on = "motif_exp"})
}

Tables generated:
\itemize{
\item \code{dea}: A table containing the DEA result (if \code{on = "exp"}, default)
\item \code{dta}: A table containing the DTA result (if \code{on = "trait_exp"})
\item \code{dma}: A table containing the DMA result (if \code{on = "motif_exp"})
}
}
\section{Dynamic Arguments}{
This step supports the following dynamic arguments:
\itemize{
\item \code{glystats.gly_ttest.p_adj_method}: P-value adjustment method (default: "BH").
\item \code{glystats.gly_ttest.ref_group}: Reference group for comparison.
\item \code{glystats.filter_sig_vars.p_adj_cutoff}: Adjusted p-value cutoff (default: 0.05).
\item \code{glystats.filter_sig_vars.p_val_cutoff}: Raw p-value cutoff.
\item \code{glystats.filter_sig_vars.fc_cutoff}: Fold change cutoff.
}
}

\examples{
step_dea_ttest()
step_dea_ttest(on = "trait_exp")  # Differential trait analysis
}
\seealso{
\code{\link[glystats:gly_ttest]{glystats::gly_ttest()}}
}
