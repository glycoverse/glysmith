% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step-preprocessing.R
\name{step_plot_qc}
\alias{step_plot_qc}
\title{Step: Plot QC}
\usage{
step_plot_qc(
  when = "post",
  batch_col = "batch",
  rep_col = NULL,
  plot_width = 7,
  plot_height = 5
)
}
\arguments{
\item{when}{Character string indicating when this QC step is run.
Use \code{"pre"} for pre-preprocessing QC, \code{"post"} for post-preprocessing QC,
or any other value for generic QC. This is appended to the step ID.
Default is \code{"post"}.}

\item{batch_col}{Column name for batch information (for \code{glyclean::plot_batch_pca()}).}

\item{rep_col}{Column name for replicate information (for \code{glyclean::plot_rep_scatter()}).}

\item{plot_width}{Width of plots in inches. Default is 7.}

\item{plot_height}{Height of plots in inches. Default is 5.}
}
\value{
A \code{glysmith_step} object.
}
\description{
Generate quality control plots for the experiment using \code{glyclean} plotting functions.
This step can be used before AND after \code{step_preprocess()} to generate QC plots at different stages.
}
\details{
Data required:
\itemize{
\item \code{exp}: The experiment to plot QC for
}

Plots generated:
\itemize{
\item \code{qc_missing_heatmap}: Missing value heatmap
\item \code{qc_missing_samples_bar}: Missing value bar plot on samples
\item \code{qc_missing_variables_bar}: Missing value bar plot on variables
\item \code{qc_tic_bar}: Total intensity count bar plot
\item \code{qc_rank_abundance}: Rank abundance plot
\item \code{qc_int_boxplot}: Intensity boxplot
\item \code{qc_rle}: RLE plot
\item \code{qc_cv_dent}: CV density plot
\item \code{qc_batch_pca}: PCA score plot colored by batch (if \code{batch_col} provided)
\item \code{qc_rep_scatter}: Replicate scatter plots (if \code{rep_col} provided)
}

When \code{when = "pre"}, plots are prefixed with \code{qc_pre_} to distinguish from post-QC plots.
When \code{when = "post"} or \code{NULL}, plots use the standard \code{qc_} prefix.
}
\section{AI Prompt}{

\emph{This section is for AI in \code{\link[=inquire_blueprint]{inquire_blueprint()}} only.}
\itemize{
\item By default, include this step ONLY after \code{step_preprocess()}.
\item You MUST provide the when parameter to specify when the QC is being run.
}
}

\examples{
step_plot_qc(when = "pre")
step_plot_qc(when = "post")
}
\seealso{
\code{\link[glyclean:plot_missing_heatmap]{glyclean::plot_missing_heatmap()}}, \code{\link[glyclean:plot_tic_bar]{glyclean::plot_tic_bar()}}, and other glyclean plotting functions.
}
