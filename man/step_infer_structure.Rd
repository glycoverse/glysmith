% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step-advanced.R
\name{step_infer_structure}
\alias{step_infer_structure}
\title{Step: Infer Glycan Structures}
\usage{
step_infer_structure(species = NULL, structure_level = "topological")
}
\arguments{
\item{species}{Species name used to restrict the glycan structure database.
Default is \code{NULL}, which does not restrict by species.}

\item{structure_level}{Structure level passed to \code{\link[glydb:glydb_structures]{glydb::glydb_structures()}}.
One of \code{"intact"}, \code{"topological"}, or \code{"basic"}.
Default is \code{"topological"}.}
}
\value{
A \code{glysmith_step} object.
}
\description{
Infer glycan structures from the \code{glycan_composition} column in \code{var_info}.
This step uses \code{glyanno::comp_to_struc()} with a structure database from
\code{glydb::glydb_structures()} and keeps only variables with an inferred
structure.

This step requires \code{exp} (experiment data).
}
\details{
Data required:
\itemize{
\item \code{exp}: The experiment whose glycan structures should be inferred
}

Data generated:
\itemize{
\item \code{uninferred_exp}: The original experiment before structure inference
}

Tables generated:
\itemize{
\item \code{inferred_structures}: A table containing the inferred structure for each
original variable and whether inference succeeded.
}
}
\section{AI Prompt}{

\emph{This section is for AI in \code{\link[=inquire_blueprint]{inquire_blueprint()}} only.}
\itemize{
\item Include this step when the user requests structure-aware analysis but the
experiment has glycan compositions and no glycan structures.
\item This step should be placed before \code{\link[=step_derive_traits]{step_derive_traits()}},
\code{\link[=step_quantify_dynamic_motifs]{step_quantify_dynamic_motifs()}}, or \code{\link[=step_quantify_branch_motifs]{step_quantify_branch_motifs()}}.
\item Mention that variables without inferred structures are removed.
\item Always ask for species restriction to improve inference accuracy,
but allow users to skip it if they want.
}
}

\examples{
step_infer_structure()
step_infer_structure(species = "Homo sapiens")
}
\seealso{
\code{\link[glyanno:comp_to_struc]{glyanno::comp_to_struc()}}, \code{\link[glydb:glydb_structures]{glydb::glydb_structures()}}
}
