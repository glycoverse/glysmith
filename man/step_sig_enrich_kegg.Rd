% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step-enrichment.R
\name{step_sig_enrich_kegg}
\alias{step_sig_enrich_kegg}
\title{Step: KEGG Enrichment Analysis on Differentially Expressed Variables}
\usage{
step_sig_enrich_kegg(
  universe = "all",
  plot_type = "dotplot",
  plot_width = 7,
  plot_height = 7,
  ...
)
}
\arguments{
\item{universe}{The universe (background) to use for enrichment analysis.
One of "all" (all genes in OrgDb), "detected" (detected variables in \code{exp}).}

\item{plot_type}{Plot type for enrichment results ("dotplot", "barplot", etc.).}

\item{plot_width}{Width of the plot in inches. Default is 7.}

\item{plot_height}{Height of the plot in inches. Default is 7.}

\item{...}{Additional arguments passed to \code{\link[glyfun:enrich_ora_kegg]{glyfun::enrich_ora_kegg()}}.}
}
\value{
A \code{glysmith_step} object.
}
\description{
Perform KEGG enrichment analysis on differentially expressed variables using \code{glyfun::enrich_ora_kegg()}.

This step requires \code{dea_res} (differential analysis result from DEA).
Run one of \code{\link[=step_dea_limma]{step_dea_limma()}}, \code{\link[=step_dea_ttest]{step_dea_ttest()}}, or \code{\link[=step_dea_wilcox]{step_dea_wilcox()}} before this step.
Only execute for glycoproteomics experiments with exactly 2 groups.
If used for glycomics experiments, the step will be skipped.
Use all genes in OrgDb as the background.
}
\details{
Data required:
\itemize{
\item \code{exp}: The experiment to perform KEGG enrichment analysis for
\item \code{dea_res}: The result from DEA, generated by \code{step_dea_xxx()}.
}

Tables generated:
\itemize{
\item \code{kegg_enrich}: A table containing the KEGG enrichment results.
}
}
\examples{
step_sig_enrich_kegg()
step_sig_enrich_kegg(plot_type = "barplot")
}
\seealso{
\code{\link[glyfun:enrich_ora_kegg]{glyfun::enrich_ora_kegg()}}
}
