# wrong dynamic arguments raise errors

    Code
      step_preprocess(preprocess.glyclean.auto_clean.remove_preset = "discovery")
    Condition
      Error in `.valid_step_dots()`:
      ! Some dynamic arguments are invalid.
      x Invalid: `preprocess.glyclean.auto_clean.remove_preset`
      i Use format `pkg.func.arg`

