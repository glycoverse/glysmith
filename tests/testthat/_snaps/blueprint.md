# blueprint checks ctx$data dependencies

    Code
      blueprint(step_volcano())
    Condition
      Error in `.validate_blueprint_dependencies()`:
      ! Blueprint is not valid due to missing step dependencies.
      x Step `step_volcano()` requires missing data: dea_res.
      i Data dea_res can be generated by `step_dea()`.

# blueprint checks overwrites

    Code
      blueprint(!!!steps)
    Condition
      Warning:
      Blueprint contains data overwrites:
      * `step_step2()` overwrites data x generated by `step_step1()`.
      i You can ignore this if this is your intention.
    Message
      
      -- Blueprint (2 steps) --
      
      * step1
      * step2

# blueprint check duplicated steps

    Code
      blueprint(step_dea(), step_dea(), step_volcano(), step_volcano())
    Condition
      Error in `.validate_blueprint_duplicates()`:
      ! Blueprint cannot contain duplicated steps.
      x `step_dea()` and `step_volcano()` are duplicated.

